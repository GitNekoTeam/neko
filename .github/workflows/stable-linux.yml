name: stable-linux

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

env:
  APP_NAME: Neko
  BINARY_NAME: neko
  OS_NAME: linux
  VSCODE_QUALITY: stable

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - vscode_arch: x64
            npm_arch: x64
          # ARM64 disabled - requires cross-compilation toolchain
          # - vscode_arch: arm64
          #   npm_arch: arm64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq libxkbfile-dev pkg-config libsecret-1-dev libkrb5-dev imagemagick

      - name: Clone VSCode
        run: . get_repo.sh

      - name: Build
        run: ./build.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VSCODE_ARCH: ${{ matrix.vscode_arch }}
          npm_config_arch: ${{ matrix.npm_arch }}
          SHOULD_BUILD: 'yes'
          CI_BUILD: 'no'

      - name: Prepare assets
        run: ./prepare_assets.sh
        env:
          VSCODE_ARCH: ${{ matrix.vscode_arch }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: neko-linux-${{ matrix.vscode_arch }}
          path: assets/*
          if-no-files-found: ignore
          retention-days: 7

  trigger-deploy:
    needs: build
    runs-on: ubuntu-latest
    if: success()
    steps:
      - name: Trigger deploy check
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'deploy.yml',
              ref: 'main'
            });
